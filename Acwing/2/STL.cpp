/*
vector 变长数组，倍增
    vector<int> a;
    vector<int> a(10,3);//定义长度为10的vector，每个数都为3
    a.size()    返回元素个数
    a.empty()   返回是否为空
    clear()     清空
    front()/back()     返回第一个数/返回最后一个数
    push_back()/pop_back()
    begin()/end()
pair<int,string>p;
    p=make_pair(10,"yxc");
    p={20,"abc"};
    first,第一个元素
    second,第二个元素
    支持比较运算，以first为第一关键字，second为第二关键字
string 字符串  
    substr(a,b)返回从a开始的b个字符
    c_str()返回字符串，用于printf
queue
    push()      向队尾插入
    front()     返回队头元素
    back()      返回队尾元素
    pop()       弹出队头元素
    q=queue<int>()  清空队列
priority_queue
    push()
    top()
    pop()
stack
    size()
    empty()
    push()
    top()
    pop()
deque效率低
    size()
    clear()
    empty()
    front()
    back()
    push_back()/pop_back()
    push_front()/pop_back()
    begin()/end()
set,map,multiset,multimap
unordered_set,unordered_map,unordered_multiset,unordered_multimap
bitset
*/
#include<iostream>
#include<algorithm>
#include<vector>

using namespace std;

int main()
{

}